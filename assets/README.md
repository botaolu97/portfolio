# Assets

Visual files used by the portfolio live in `visuals/`.

Replace the SVG preview files with Figma exports when final project imagery is ready. Keep the filenames stable if you want the pages to update without editing HTML.

The public resume PDF lives at `resume.pdf`.
